---
import Section from "./section.astro";
const {mt, mb, ml, mr } = Astro.props;
import Text from "../components/text.astro";
let marginTop = "mt-none";
let marginBottom = "mb-none";
let marginLeft = "ml-none";
let marginRight = "mr-none";
if (mt === "true") {
    marginTop = "mt-4";
}
if (mb === "true") {
    marginBottom = "mb-4";
}
if (ml === "true") {
    marginLeft = "ml-4";
}
if (mr === "true") {
    marginRight = "mr-4";
}
---
<div class={`flex flex-row ${marginTop} ${marginBottom} ${marginLeft} ${marginRight} w-fit w-max-156 p-2 rounded bg-tertiaryBackground-light dark:bg-tertiaryBackground-dark`}>
    <Section orientation="row" importance="primary" align="left">
        <Text text="Consultorios Cercanos" type="title"/>
        <Text text="Aqui podras navegar por los consultorios a tu alrededor" type="text"/>
        <Section extraClass="w-128" orientation="column">
            <slot/>
        </Section>
    </Section>
</div>