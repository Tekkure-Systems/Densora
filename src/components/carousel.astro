---
const {importance, mt, mb, orientation, align, extraClass, padding} = Astro.props;
let containerClass = "";
let marginTop = "mt-none";
let marginBottom = "mb-none";
let flexOrientation = "";
let alignment = "";
let p = "p-4";
if (mt == "true") {
    marginTop = "mt-4";
}
if (mb == "true") {
    marginBottom = "mb-4";
}
if(importance == "primary") {
    containerClass = "bg-primaryBackground-light dark:bg-primaryBackground-dark";
}
if(importance == "secondary") {
    containerClass = "bg-secondaryBackground-light dark:bg-secondaryBackground-dark";
}
if(orientation == "col"){
    flexOrientation = "flex-row";
}
if(orientation == "row"){
    flexOrientation = "flex-col";
}
if (align == "center") {
    alignment = "items-center"
}
if (align == "left") {
    alignment = "items-start"
}
if (align == "right") {
    alignment = "items-end"
}
if (padding == "false") {
    p = "p-none"
}
---
<div class={`flex ${flexOrientation} ${marginTop} ${marginBottom} ${alignment} ${extraClass} ${p} ${containerClass} rounded overflow-y-auto max-h-[80vh]`}>
    <slot />
</div>
